{
  "default": {
    "identifier": "default",
    "description": "Flowmap makes no network calls. The only outward action is opening an https: link in the system browser.",
    "local": true,
    "windows": ["main"],
    "permissions": ["core:default", "opener:allow-open-url"]
  }
}
