{
  "name": "@flowmap/fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Validation, scale, edge, and import fixtures. One stable dataset for design, tests, demos, and benchmarks.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b",
    "build": "tsc -b"
  },
  "dependencies": {
    "@flowmap/domain": "workspace:*",
    "@flowmap/testing": "workspace:*"
  }
}
