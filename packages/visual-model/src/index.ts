/**
 * @flowmap/visual-model — layout-neutral view models, selection and focus
 *
 * Not implemented yet. Scheduled for M2 (backlog M2-MAP-*).
 * Contract: docs/spec/06-views-interaction.md
 *
 * The package exists now so the dependency graph, boundary rules, and CI matrix
 * are real from the first commit rather than retrofitted.
 */

export const PACKAGE_NAME = '@flowmap/visual-model';
export const IMPLEMENTED = false;
