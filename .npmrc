# Fail fast rather than silently hoisting: boundary rules only work if imports are explicit.
strict-peer-dependencies=false
auto-install-peers=true
# No lifecycle scripts from dependencies — supply-chain control, see docs/spec/10-desktop-security.md
enable-pre-post-scripts=false
